# examples/ — worked outputs

Real, end-to-end demonstrations of the skills used together. These show the intended workflow:
**generate → review → ship**, and how the skills chain.

- `season-1-launch/` — a capstone: an objective runs through `campaign-strategist` →
  `brand-writer` → `image-art-director`, then the outputs are checked by `copy-reviewer` and
  `image-reviewer`. *(added in the final build step)*
